<div class="selected-role-container">
    <div class="selected-role-card">
        <a href="/role/{{role.id}}">{{role.role_name}}</a> 
        <p>{{role.company}}</p>
        <p>£{{role.salary}} per annum</p>
        <p>{{role.job_type}}</p>
        <p>{{role.location.location_name}}</p>
        <p>{{role.category.category_name}}</p>
        <p>{{role.description}}</p>
        <a href="/application/{{role.id}}"><button class="applybtn button">Apply Now</button></a>
       <!--**--> <button id="savebtn" class="savebtn button" data-id="{{role.id}}">Save Job</button>  <!--js when they click save it makes a post request to /api/savedRole/(creates a savedRole record) add javascript functionality so that when they click save the button changes to 'saved' if response.ok saved +refresh page-->
    </div>
</div>



<script src="/js/role.js"></script> <!--**-->